{
  "id": "agent-rbac",
  "name": "agent-rbac",
  "description": "Adaptive RBAC, audit, and trust copilot for OpenClaw agents",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "permissionsConfigPath": {
        "type": "string"
      },
      "stateDir": {
        "type": "string"
      },
      "openClawStateDir": {
        "type": "string"
      },
      "sessionStorePath": {
        "type": "string"
      },
      "locale": {
        "type": "string"
      },
      "promptGuard": {
        "type": "boolean"
      },
      "toolGuard": {
        "type": "boolean"
      },
      "gatewayMethods": {
        "type": "boolean"
      },
      "maxSmokeSubagents": {
        "type": "number"
      },
      "defaultUserIdStrategy": {
        "type": "string",
        "enum": ["session-origin", "session-key", "session-id"]
      }
    },
    "required": ["permissionsConfigPath", "stateDir"]
  },
  "uiHints": {
    "permissionsConfigPath": {
      "label": "Permissions Config Path",
      "help": "Absolute or ~-relative path to the permission config JSON.",
      "placeholder": "~/.openclaw/agent-rbac.permissions.json"
    },
    "stateDir": {
      "label": "State Directory",
      "help": "Directory for audit, adaptive, and memory state.",
      "placeholder": "~/.openclaw/agent-rbac-state"
    },
    "openClawStateDir": {
      "label": "OpenClaw State Directory",
      "help": "Root OpenClaw state dir used to discover session metadata for external identity mapping.",
      "placeholder": "~/.openclaw"
    },
    "sessionStorePath": {
      "label": "Session Store Path",
      "help": "Optional explicit sessions.json path. Overrides automatic discovery from OpenClaw state dir."
    },
    "promptGuard": {
      "label": "Prompt Guard",
      "help": "Inject refusal or boundary guidance before model prompt build."
    },
    "toolGuard": {
      "label": "Tool Guard",
      "help": "Evaluate tool calls before execution and block denied calls."
    },
    "gatewayMethods": {
      "label": "Gateway Methods",
      "help": "Expose audit, evaluate, and smoke-test gateway methods."
    },
    "defaultUserIdStrategy": {
      "label": "Default User ID Strategy",
      "help": "Default is session-origin, which derives stable external identities from OpenClaw session origin metadata before falling back to session identifiers."
    }
  }
}
